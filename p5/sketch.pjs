function setup() {
  createCanvas(windowWidth, windowHeight);
  noStroke();
  fill(255, 0, 0);
}

function draw() {
//  background(255);
  ellipse(mouseX,mouseY,75,100);
	rect(300,200,50,50);
}

function mousePressed(){
	fill(75,65,140);
	rect(pmouseX,pmouseY,75,100);
	return false;
}
